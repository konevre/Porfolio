import React from "react";
import ContactComponent from "../components/Contact/ContactComponent";

const ContactPage = ({ state }) => {
    return <ContactComponent state={state} />;
};
export default ContactPage;
